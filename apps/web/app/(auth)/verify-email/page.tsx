'use client';import {useEffect,useState} from 'react';import {api} from '@/lib/api';import Link from 'next/link';export default function Verify(){const [message,setMessage]=useState('Verifying your email…');useEffect(()=>{const token=new URLSearchParams(location.search).get('token');if(!token){setMessage('Verification token is missing.');return}api.post('/auth/verify-email',{token}).then(()=>setMessage('Email verified. You can now sign in.')).catch(e=>setMessage(e.response?.data?.message||'Verification failed.'))},[]);return <main className="grid min-h-screen place-items-center p-5"><div className="glass rounded-2xl p-7 text-center"><h1 className="text-2xl font-semibold">Email verification</h1><p className="mt-3 text-zinc-500">{message}</p><Link className="mt-5 inline-block text-primary" href="/login">Go to sign in</Link></div></main>}
