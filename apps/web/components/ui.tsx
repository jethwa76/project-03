'use client';import {cn} from '@/lib/utils';import {Loader2,Sun,Moon} from 'lucide-react';import {useTheme} from 'next-themes';import type {ButtonHTMLAttributes,InputHTMLAttributes} from 'react';
export function Button({className,loading,...p}:ButtonHTMLAttributes<HTMLButtonElement>&{loading?:boolean}){return <button {...p} disabled={loading||p.disabled} className={cn('inline-flex items-center justify-center rounded-lg bg-primary px-4 py-2 text-sm font-medium text-white transition hover:opacity-90 disabled:cursor-not-allowed disabled:opacity-50',className)}>{loading&&<Loader2 className="mr-2 h-4 w-4 animate-spin"/>}{p.children}</button>}
export function Input(p:InputHTMLAttributes<HTMLInputElement>){return <input {...p} className={cn('w-full rounded-lg border bg-transparent px-3 py-2 text-sm placeholder:text-zinc-500',p.className)}/>}
export function Card({children,className}:{children:React.ReactNode;className?:string}){return <section className={cn('glass rounded-2xl p-5 shadow-glass',className)}>{children}</section>}
export function ThemeToggle(){const {theme,setTheme}=useTheme();return <Button aria-label="Toggle theme" onClick={()=>setTheme(theme==='dark'?'light':'dark')} className="bg-transparent p-2 text-foreground hover:bg-zinc-500/10">{theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}</Button>}
export function Skeleton({className}:{className?:string}){return <div className={cn('animate-pulse rounded bg-zinc-300/40 dark:bg-zinc-700/50',className)}/>}
