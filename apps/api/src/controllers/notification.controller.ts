import type {RequestHandler} from 'express'; import {prisma} from '../config/prisma.js'; export const listNotifications:RequestHandler=async(req,res,next)=>{try{const data=await prisma.notification.findMany({where:{userId:req.user!.id},orderBy:{createdAt:'desc'},take:50});res.json({success:true,data})}catch(e){next(e)}}; export const markRead:RequestHandler=async(req,res,next)=>{try{await prisma.notification.updateMany({where:{id:req.params.id,userId:req.user!.id},data:{readAt:new Date()}});res.json({success:true})}catch(e){next(e)}};
