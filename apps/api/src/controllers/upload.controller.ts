import type {RequestHandler} from 'express'; import {v2 as cloudinary} from 'cloudinary'; import {prisma} from '../config/prisma.js'; import {AppError} from '../utils/app-error.js'; import {env} from '../config/env.js';
if(env.CLOUDINARY_CLOUD_NAME) cloudinary.config({cloud_name:env.CLOUDINARY_CLOUD_NAME,api_key:env.CLOUDINARY_API_KEY,api_secret:env.CLOUDINARY_API_SECRET});
export const upload:RequestHandler=async(req,res,next)=>{try{if(!req.file)throw new AppError(400,'A file is required');if(!env.CLOUDINARY_CLOUD_NAME)throw new AppError(503,'File uploads require Cloudinary configuration');const result=await new Promise<any>((resolve,reject)=>cloudinary.uploader.upload_stream({resource_type:'auto',folder:'project3'},(e,r)=>e?reject(e):resolve(r)).end(req.file!.buffer));const file=await prisma.fileAsset.create({data:{url:result.secure_url,publicId:result.public_id,name:req.file.originalname,mimeType:req.file.mimetype,size:req.file.size,recordId:req.body.recordId||null,uploadedById:req.user!.id}});res.status(201).json({success:true,data:file})}catch(e){next(e)}};
