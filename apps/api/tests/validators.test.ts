import {describe,it,expect} from 'vitest';import {signupSchema,recordSchema} from '../src/validators/schemas.js';describe('validation schemas',()=>{it('rejects weak passwords',()=>expect(()=>signupSchema.parse({body:{email:'a@b.com',password:'weak',firstName:'A',lastName:'B'}})).toThrow());it('normalizes record reference',()=>expect(recordSchema.parse({body:{title:'Launch plan',reference:'prj-7'}}).body.reference).toBe('PRJ-7'))});
